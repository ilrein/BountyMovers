import React, { Component } from 'react';

export default class Map extends Component {
  constructor(props) {
    super(props);

    this.state = {};
    console.log(google);
  }

  render() {
    return (
      <div id="map"></div>
    );
  }
}
